<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario  
            h1 Coloca tu nuevo password
                

            form#formulario.caja.caja-login(method="POST")
                .campo 
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu contraseña" value= password required)
                .campo.enviar 
                    input.boton(type="submit" value="reestablecer password")

        